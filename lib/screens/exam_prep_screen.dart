import 'package:flutter/material.dart';
class ExamPrepScreen extends StatelessWidget { const ExamPrepScreen({super.key}); @override Widget build(BuildContext context) { return Scaffold(appBar: AppBar(title: const Text('Exam Prep')), body: const Center(child: Text('Exam Prep'))); } }
